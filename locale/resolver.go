package locale

// Resolve is @todo implement
// lib/internal/Magento/Framework/Locale/Resolver
func Resolve() {

	// get locale from currecnt loaded store via path general/locale/code and the depending scope (store or website)
	// ... if not possible then fall back to default locale en_US
}
